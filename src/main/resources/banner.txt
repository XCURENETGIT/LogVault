
---------------------------------------------------------------------------------------------------------------------------------------------
Application : ${spring.application.name}
Description : ✅ File Ingestion and Metadata Persistence 1. Storing Uploaded Files 2. Extracting and Saving Metadata 3. Optional Features
Version     : ${server.version}.${release.version}
JAVA        : ${java.runtime.name} ${java.version}
OS          : ${os.name}.${os.version}
PID         : ${PID}
Port        : ${server.port}
Log Level   : ${logging.level.root}
Log History : ${logging.logback.rollingpolicy.max-history} day
---------------------------------------------------------------------------------------------------------------------------------------------