
---------------------------------------------------------------------------------------------------------------------------------------------
Application : ${spring.application.name}
Description : ✅ A module that processes and stores usage records of the generative AI service, handling tasks such as file analysis, PII detection
Version     : ${server.version}.${release.version}
JAVA        : ${java.runtime.name} ${java.version}
OS          : ${os.name}.${os.version}
PID         : ${PID}
Port        : ${server.port}
---------------------------------------------------------------------------------------------------------------------------------------------